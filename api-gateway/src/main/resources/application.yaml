server:
  port: 8080

application:
  title: Api gateway
  version: SNAPSHOT-1.0
spring-boot:
  version: 2.5.7

spring:
  application:
    name: API-GATEWAY

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/